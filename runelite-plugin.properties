displayName=Camera Locks
author=varzeki
support=https://github.com/Varzeki/camera-locks/issues
description=Lock the camera in place and save/restore named camera position presets
tags=camera,lock,zoom,pan,preset
plugins=com.cameralock.CameraLockPlugin
version=
build=standard
